{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "gcc - Build and debug active file with args",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",

            "args": ["ls -l", "r"], 
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "preLaunchTask": "C/C++: gcc-15 build active file" 
        }
    ]
}